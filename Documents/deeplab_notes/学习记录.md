# 学习记录

## 目录

- [学习记录](#学习记录)
  - [目录](#目录)
  - [Git \& GitHub 专栏](#git--github-专栏)
    - [基本概念与常用命令](#基本概念与常用命令)
    - [日常操作流程](#日常操作流程)
    - [分支与协作](#分支与协作)
    - [进阶问题与经验](#进阶问题与经验)
    - [近期知识与纠错](#近期知识与纠错)
  - [VSCode 专栏](#vscode-专栏)
    - [基础与配置](#基础与配置)
  - [语义分割任务专栏](#语义分割任务专栏)
    - [数据集处理](#数据集处理)
    - [训练与评估流程](#训练与评估流程)
    - [近期知识与纠错](#近期知识与纠错-1)
  - [Deeplab模型专栏](#deeplab模型专栏)
    - [结构理解与自定义](#结构理解与自定义)
    - [关键代码与经验](#关键代码与经验)
  - [常见问题与经验](#常见问题与经验)
  - [选题与实验思路](#选题与实验思路)
  - [自动化与脚本清单](#自动化与脚本清单)

---

## Git & GitHub 专栏

### 基本概念与常用命令

- 本地仓库、远程仓库、分支、提交、推送等基础概念。
- 常用命令：init、add、commit、push、pull、branch、merge、checkout、reset、stash、.gitignore、README.md、.git 文件夹作用等。
- 版本回退与历史查找（log、reflog、checkout、reset）
- 暂存区（add、reset、stash）、什么是暂存

### 日常操作流程

1. 编辑文件
2. 保存（Ctrl+S）
3. `git add .`
4. `git commit -m "说明"`
5. `git push`

### 分支与协作

- main/master 分支管理，删除分支、合并、冲突解决。
- GitHub 网页端分支管理、PR、Issue、协作流程。

### 进阶问题与经验

- 参数唯一来源方案（参数表驱动训练/评估，消除歧义）
- 代码链路 debug 经验
- `.gitignore` 文件用于忽略不需要上传的文件。
- GitHub 网页端可管理分支、设置默认分支、删除分支。

### 近期知识与纠错

- token_length 参数链路与 shape mismatch 问题：训练、评估时 token_length 需严格一致，否则权重加载 shape mismatch。
- 建议以参数表（experiment_records.csv/config.txt）为唯一参数源，训练、评估均从参数表读取，彻底消除歧义和 shape mismatch。
- 发现 DeepLab __init__ 参数顺序与调用不一致，需全部用关键字参数传递。
- 彻底修复 DeepLab/DeeplabV3 参数链路，去除所有默认值，强制参数表唯一来源。

---

## VSCode 专栏

### 基础与配置

- 工作区、.vscode 配置、launch.json、settings.json
- SSH 配置与远程开发
- 插件推荐与使用技巧
- .ssh 的定义、用途、使用方法
- 常见报错与排查
- VSCode 需安装 Microsoft 官方 Python 扩展，左下角选择具体解释器路径（如虚拟环境、Anaconda）。
- `.vscode/launch.json` 配置便于一键运行/调试当前 Python 文件。
- 保证 PyCharm 与 VSCode 使用同一虚拟环境或解释器，依赖包一致，路径设置一致。

---

## 语义分割任务专栏

### 数据集处理

- Cityscapes/VOC 标签映射、格式转换、数据划分
- 数据一致性检查脚本、标签合法性检查
- 数据集下载、权重文件管理、环境配置建议
- 用 `auto_copy_cityscapes_to_voc.py` 自动补全 VOC 缺失的标签和图片，支持图片格式转换。
- 用 `check_missing_labels.py` 检查 VOC 标签和图片缺失情况。
- `gen_voc_train_txt.py`、`gen_voc_val_txt.py` 生成 VOC 格式的 train/val 划分。
- `halve_voc_split_and_clean.py` 可将 train.txt/val.txt 数量减半，并同步清理未用图片和标签。
- `check_label_values.py` 随机抽查标签文件，自动判断 trainId/labelId 类型，检查像素值是否合法，辅助定位标签映射问题。

### 训练与评估流程

- 训练参数设置、损失函数、评估指标
- 消融实验设计与记录（experiment_records.csv 维护）
- 结果可视化与自动化分析
- mIoU 过低排查、数据/模型/流程一致性
- 训练日志、权重、评估结果建议分主干/实验单独保存，便于对比和复现。
- 自动化脚本与批量处理建议
- 训练、验证、评估流程现已与Cityscapes官方兼容。
- 后续如需自定义类别或迁移其他数据集，务必同步调整num_classes及标签映射。

### 近期知识与纠错

- 标签处理完全符合 Cityscapes 官方标准。
- 训练和评估均可直接对接官方脚本。
- 检查并确认评估脚本参数：get_miou.py 中 num_classes=19，name_classes 顺序与 Cityscapes 官方19类严格一致。
- 训练、验证、评估流程全部与 Cityscapes 官方标准严格一致。
- 训练前建议用脚本检查数据集完整性，避免训练中断。
- 训练日志、权重、评估结果建议分主干/实验单独保存，便于后续对比和复现。

---

## Deeplab模型专栏

### 结构理解与自定义

- Deeplab 主干、tokens机制、特征可视化
- 个人模型改进与思考
- 在 deeplab.py 的 DeeplabV3 类中新增 use_tokens 参数，支持 tokens 机制的消融实验（可开关 tokens 相关优化）。
- 训练与推理时可通过 use_tokens 控制是否启用 tokens 机制，便于对比实验。
- generate() 方法中自动注册 forward hook，捕获 tokens 层输出，便于特征可视化与分析。
- save_tokens_feature 方法，可将 tokens 层特征图（前4通道）保存为图片，支持可视化分析。

### 关键代码与经验

- 关键参数、结构、消融实验、特征可视化方法
- 训练、评估、可视化全流程标准化
- 明确所有数据处理、模型结构、损失函数、评估流程均严格对齐 Cityscapes 官方标准（如 labelId→trainId 映射、ignore=255、num_classes=19、类别顺序等）。
- 训练脚本 train.py、评估脚本 get_miou.py、dataloader0.py、deeplabv3_training.py 等均已修正并记录。

---

## 常见问题与经验

- 代码与数据集下载、权重文件问题、环境配置、GPU利用、训练与评估常见报错、其它经验等。
- OOM（显存溢出）：调小 batch_size 或 input_shape，或减少 num_workers。
- 权重/日志/miou 路径错乱：建议统一规范目录结构，避免不同主干/实验混用同一目录。
- 标签文件缺失/损坏：用 check_label_values.py、check_label_validity.py 等脚本批量检查。
- 训练/验证集类别分布极不均衡：建议用 auto_split_train_val.py 自动划分，保证评估科学性。
- 训练前建议用脚本检查数据集完整性，避免训练中断。
- 训练日志、权重、评估结果建议分主干/实验单独保存，便于后续对比和复现。
- 如遇新问题，优先查阅项目文档和历史记录，再针对性排查数据、模型、训练流程，记录每次实验参数与结果。

---

## 选题与实验思路

- 城市道路语义分割选题简介、实验思路、可扩展方向、结果分析与展示、其它建议。
- 先用DeepLabV3+主干（mobilenetv2/xception）做基础实验，熟悉训练、评估、miou等流程。
- 尝试不同主干、结构（如加注意力模块、换轻量主干等），对比精度和推理速度。
- 关注小众类别的表现，合理设置类别权重，提升整体miou。
- 训练和评估流程建议全部自动化，权重、日志、miou输出分目录保存，便于后续对比。
- 训练前务必检查数据集和标签完整性，避免训练中断。
- 训练参数（batch_size、input_shape、num_workers等）要结合显存实际调整，优先保证训练稳定。
- 如遇新问题，建议先查阅项目文档和历史记录，再针对性排查。

---

## 自动化与脚本清单

- 标签与预测类别空间检查、文件名与shape一致性、可视化对比分析、评估与混淆矩阵、数据集一致性与清理、训练与评估全流程标准化等自动化脚本说明与经验。
- check_label_values.py：批量检查标签像素值分布，自动判断trainId/labelId类型，辅助定位标签映射问题。
- check_pred_unique.py：批量检查模型预测输出的unique分布，确认类别空间与标签一致。
- check_label_pred_pair.py：自动检查并修正标签与预测文件名、shape一一对应，生成detection-results_fixed目录，保证评估流程严格对齐。
- visualize_label_pred.py：批量可视化标签与预测，输出对比图到visualize_compare，直观分析模型分割结构与标签一致性。
- get_miou.py：自动评估mIoU、mPA、Accuracy、混淆矩阵，支持参数同步与可视化配色，便于定位类别错位或模型无效问题。
- check_voc_integrity.py：自动检查VOC数据集图片、标签与split文件一致性，生成批量清理脚本，保证数据集结构规范。
- train.py：支持命令行参数与自动权重统计，训练/评估/日志/权重输出全自动化，便于批量实验与复现。
- 若新模型mIoU异常，优先用check_label_values.py、check_pred_unique.py、check_label_pred_pair.py排查类别空间、文件名、shape、类别顺序等一致性。
- 用visualize_label_pred.py批量可视化分析模型分割结构与标签的对应关系，直观判断模型是否有效学习。
- 评估流程(get_miou.py)同步参数与配色，结合混淆矩阵定位类别错位或模型无效。
- 所有脚本均支持批量处理与自动修正，保证每次实验可复现、可追溯。
- 建议每次实验后及时补充本经验记录，归档脚本与典型问题排查流程。
- 持续完善自动化脚本，支持一键全流程检查、修正、可视化与评估。
- 典型排查流程可整理为shell批处理或python主控脚本，提升复现与迁移效率。
